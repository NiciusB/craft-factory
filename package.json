{
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "run-ai-gen": "yarn workspace ai-icon-generator generate-icons"
  },
  "devDependencies": {
    "pre-commit": "^1.2.2",
    "turbo": "^1.6.3"
  },
  "pre-commit": ["lint"],
  "workspaces": [
    "apps/*"
  ]
}
